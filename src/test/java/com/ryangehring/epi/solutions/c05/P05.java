package com.ryangehring.epi.solutions.c05;

/**
 * Bit operations only multiplication.
 * Slow way --- I could replicate the
 * same operations we are taught in elementary school
 * multiplications, digit by digit. This is O(n) * O(m) complexity if we went bit by bit.
 *
 */
public class P05 {

    public static int multiply(int x, int y ) {
        
    }
}
