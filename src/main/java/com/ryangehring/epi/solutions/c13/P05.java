package com.ryangehring.epi.solutions.c13;

/**
 * Assume you start at each node.
 * Get the parent for each iterating lockstep.
 * Check the cache.
 * This is O(2(height difference)) = O(depth difference)
 * Space usage is 2(height difference.)
 * Not gonna bother writing it out... its just caching.
 */
public class P05 {
}
