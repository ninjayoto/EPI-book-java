package com.ryangehring.epi.solutions.c19;

/**
 * Flip the color of a region.
 * This one is really similar to the maze, I'm going to skip it.
 * Just iterate over the possible moves in the matrix. Flip
 * the color at every point if it equals the correct start color (and
 * queue the adjacent squares). Terminate on empty queue.
 *
 */
public class P02 {
}
